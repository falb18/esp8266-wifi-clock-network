You can easily update the clock via OTA. OTA binary is attached with every new firmware release.

navigate to clock's ip address /update, for example: 192.168.0.10/update you will be asked for credentials, by default it is: user: nick password: nick select the .bin file and hit "Update firmware" wait for the clock to reboot, done!